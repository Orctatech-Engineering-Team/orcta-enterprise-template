import { createContainer } from "@bootstrap/Container";

// Export the container for use by the app
export { createContainer };
export type { AppConfig, AppContainer, Environment } from "@bootstrap/Container";
